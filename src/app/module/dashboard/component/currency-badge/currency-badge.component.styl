logo_size = 4em;

:host {
  display: flex;
  flex-direction: row;
  // background-color: #00000010;
}

header {
  display: flex;
  flex-direction: row;

  .logo {
    &-wrapper {
      display: flex;
      width: logo_size;
      height: logo_size;
      align-items: center;
      justify-content: center;
    }

    &-icon {
      max-width: 100%;
      max-height: 100%;
    }
  }
}

.synopsis {
  display: flex;
  flex-direction: column;
  align-items: flex-start;
}